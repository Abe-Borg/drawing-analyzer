"""Test fixtures for Spec Critic.

The ``fake_anthropic`` module is the primary entry point — it builds the
fake Anthropic response shapes the production parsers consume so tests
never need a real API key or network connection.
"""
