# requirements.txt — pinned runtime dependencies to install and run
# drawing-analyzer (engine + GUI), generated from `pip install -e ".[gui]"`
# followed by `pip freeze --exclude-editable` (Python 3.11).
#
# This installs the library only; it does NOT install the project itself.
# To run the software, install the package too:
#     pip install -r requirements.txt -e .
# (or just `pip install -e ".[gui]"`, which pyproject.toml drives directly).
#
# For a hermetic engine + test setup instead of the GUI, see
# requirements-release.lock (pip install -c requirements-release.lock ".[dev]").

# --- engine (drawing_analyzer core) ---
annotated-types==0.8.0
anthropic==1.4.0
anyio==4.15.1
certifi==2026.7.22
charset-normalizer==3.5.1
docstring_parser==0.18.0
h11==0.16.0
httpcore2==2.12.0
httpx2==2.12.0
idna==3.19
jiter==0.16.0
lxml==6.1.3
packaging==26.3
platformdirs==4.11.7
pydantic==2.13.5
pydantic_core==2.48.0
pymupdf==1.28.2
pypdf==6.18.0
python-docx==1.2.0
regex==2026.9.3
requests==2.34.2
sniffio==1.3.1
tiktoken==0.14.0
truststore==0.10.4
typing-inspection==0.4.4
typing_extensions==4.16.0
urllib3==2.7.0

# --- gui extra (`pip install -e ".[gui]"`) ---
customtkinter==6.0.0
darkdetect==0.8.0
tkinterdnd2==0.6.3

# keyring (credential-safe API-key store) and its transitive deps
keyring==25.7.0
jaraco.classes==3.4.0
jaraco.context==6.1.2
jaraco.functools==4.6.0
more-itertools==11.1.0

# keyring's Secret Service backend (Linux only)
SecretStorage==3.5.0; sys_platform == "linux"
jeepney==0.9.0; sys_platform == "linux"
cryptography==50.0.1; sys_platform == "linux"
cffi==2.1.1; sys_platform == "linux"
pycparser==3.0; sys_platform == "linux"

# keyring's Windows Credential Manager backend (Windows only)
pywin32-ctypes==0.2.3; sys_platform == "win32"

# importlib.metadata backport, needed on Python < 3.12
importlib_metadata==9.0.1; python_version < "3.12"
zipp==4.1.0; python_version < "3.12"
backports.tarfile==1.2.0; python_version < "3.12"
